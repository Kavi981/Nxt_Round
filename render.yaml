services:
  - type: web
    name: nxt-round-backend
    env: node
    buildCommand: npm run build-all
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000 